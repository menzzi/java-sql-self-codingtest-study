```SQL
WITH RECURSIVE T1 AS (
    SELECT 0 AS HOUR
    
    UNION ALL
    
    SELECT HOUR + 1
    FROM T1
    WHERE HOUR < 23
)

SELECT HOUR, COUNT(ANIMAL_ID) AS COUNT
FROM T1 T1 LEFT JOIN ANIMAL_OUTS AO ON T1.HOUR = HOUR(AO.DATETIME)
GROUP BY HOUR
ORDER BY HOUR
```
