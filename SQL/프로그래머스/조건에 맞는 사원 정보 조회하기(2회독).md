```SQL
SELECT SCORE, HE.EMP_NO, EMP_NAME, POSITION, EMAIL
FROM HR_DEPARTMENT HD JOIN HR_EMPLOYEES HE ON HD.DEPT_ID = HE.DEPT_ID
JOIN (SELECT SUM(SCORE) AS SCORE, YEAR, EMP_NO FROM HR_GRADE GROUP BY YEAR, EMP_NO) AS HG ON HE.EMP_NO = HG.EMP_NO
WHERE YEAR = '2022'
ORDER BY SCORE DESC
LIMIT 1
```
